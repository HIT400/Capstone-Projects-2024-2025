import 'package:flutter/material.dart';
import 'package:font_awesome_flutter/font_awesome_flutter.dart';

class Shimmers {
  static const num = Card(
      child: SizedBox(height: 20, width: 50));
  static const headline = Card(
      child: SizedBox(height: 50, width: 200));
  static const title = Card(
      child: SizedBox(height: 20, width: 150));
  static const subtitle = Card(
      child: SizedBox(height: 10, width: 150));
  static const ellipsis = Icon(FontAwesomeIcons.ellipsisVertical);
  static const box = Card();
}