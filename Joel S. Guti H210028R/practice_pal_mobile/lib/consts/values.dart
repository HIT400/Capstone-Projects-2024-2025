import 'package:flutter/material.dart';

class AppValues {
  static var shape = RoundedRectangleBorder(
      borderRadius: BorderRadius.circular(10));

  static const mightTakeAWhile = "This might take a while";
  static const wait = "Please wait";
}